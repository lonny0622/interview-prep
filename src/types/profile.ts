export type StructuredProfile = { candidate: { name: string; headline: string; yearsExperience: number; skills: string[]; experiences: Array<{ company: string; title: string; period: string; responsibilities: string[] }>; projects: Array<{ name: string; background: string; responsibilities: string[]; techStack: string[]; challenges: string[]; solutions: string[]; results: string[]; risks: string[] }>; sourceText?: string }; job: { role: string; responsibilities: string[]; requiredSkills: string[]; preferredExperience: string[]; interviewSignals: string[]; sourceText?: string }; gaps: string[] }
export type ResumeProfile = { id: string; role: string; fileName: string; text: string; candidateProfile?: StructuredProfile | null; parsedAt?: string | null }
export type UserProfile = { id: number; name: string; headline: string; yearsExperience: number; targetRoles: string[]; resumeText: string; resumeFileName: string; resumes: ResumeProfile[]; candidateProfile?: StructuredProfile | null; parsedAt?: string | null }
